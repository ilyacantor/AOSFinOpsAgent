# =============================================================================
# AOS FinOps Agent — Environment Variables
# Copy to .env and fill in values. Secrets go in Render dashboard.
# Note: FinOps uses Node.js/Express (anomaly — all other AOS modules use Python/FastAPI)
# =============================================================================

# --- Database (Neon PostgreSQL) ---
DATABASE_URL=                              # Full PostgreSQL connection string
PGHOST=                                    # Neon host
PGDATABASE=neondb                          # Database name
PGUSER=                                    # Database user
PGPASSWORD=                                # Database password
PGPORT=5432                                # Database port

# --- AI / LLM ---
GEMINI_API_KEY=                            # Google Gemini API key
PINECONE_API_KEY=                          # Pinecone vector DB key

# --- DCL Integration ---
DCL_URL=/api/unified_data                  # DCL endpoint path

# --- Auth ---
SESSION_SECRET=                            # Session signing key
